const elCartContent=document.getElementById("cartcontent"),elCartPrice=document.getElementById("cartprice");var panier=JSON.parse(Cookies.get("cart")),prixTotal=0;panier.forEach(t=>{prixTotal+=t.tarif*t.quantite}),elCartPrice.value=prixTotal,panier.forEach(t=>{elCartContent.value+=t.id+"x"+t.quantite+";"});