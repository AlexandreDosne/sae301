const COOKIE_PREFIX="cart=",COOKIE_SUFIX=";path=/",elCartList=document.getElementById("cartList");var articles=[],panier="";0!=document.cookie.length&&(articles=JSON.parse(document.cookie)),elCartList.innerHTML=articles,articles.forEach(e=>{panier+="["+e+"] "});